git init.

git config --global user.name "G.Sai Srikar"

git config --global user.email "2400032347@kluniversity.in"

git add .

git commit -m "commited"

git branch -M main
 
git remote add origin.

git push -U main.

git checkout

git log
